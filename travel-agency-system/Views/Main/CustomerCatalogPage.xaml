<Page x:Class="travel_agency_system.Views.Main.CustomerCatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:travel_agency_system.Views.Main"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="Tour Catalog" Loaded="Page_Loaded">

    <materialDesign:DialogHost x:Name="RootDialog" CloseOnClickAway="True">

        <materialDesign:DialogHost.DialogContent>
            <StackPanel Margin="30" Width="250">
                <TextBlock Text="Top Up Balance" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <TextBox x:Name="TxtTopUpAmount" 
                         materialDesign:HintAssist.Hint="Enter amount ($)" 
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                         PreviewTextInput="NumberValidationTextBox"
                         FontSize="16" Margin="0,0,0,30"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="CANCEL" 
                            Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" 
                            Style="{StaticResource MaterialDesignFlatButton}" 
                            Margin="0,0,10,0" Foreground="Gray"/>

                    <Button Content="CONFIRM" 
                            Click="BtnConfirmTopUp_Click" 
                            Style="{StaticResource MaterialDesignRaisedButton}"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:DialogHost.DialogContent>

        <Grid Background="{DynamicResource MaterialDesignPaper}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <materialDesign:ColorZone Mode="PrimaryMid" materialDesign:ElevationAssist.Elevation="Dp3">
                <Border Padding="16">
                    <DockPanel>
                        <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center">

                            <Button Style="{StaticResource MaterialDesignToolButton}" 
                                    Click="BtnHistory_Click" 
                                    ToolTip="Purchase History" 
                                    Margin="0,0,15,0">
                                <materialDesign:PackIcon Kind="History" Width="28" Height="28" Foreground="White"/>
                            </Button>

                            <materialDesign:PackIcon Kind="Wallet" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="White"/>
                            <TextBlock x:Name="txtUserBalance" Text="Balance: 0.00$" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Foreground="White"/>

                            <Button Style="{StaticResource MaterialDesignToolButton}" 
                                    Click="BtnTopUp_Click" 
                                    ToolTip="Top Up Balance" 
                                    Margin="5,0,0,0">
                                <materialDesign:PackIcon Kind="PlusCircleOutline" Width="28" Height="28" Foreground="#81C784"/>
                            </Button>

                            <Button Style="{StaticResource MaterialDesignToolButton}" 
                                    Click="BtnLogout_Click" 
                                    ToolTip="Logout" 
                                    Margin="15,0,0,0">
                                <materialDesign:PackIcon Kind="Logout" Width="28" Height="28" Foreground="White"/>
                            </Button>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Compass" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="AVAILABLE TOURS" FontSize="22" FontWeight="ExtraBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DockPanel>
                </Border>
            </materialDesign:ColorZone>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="10">
                <ItemsControl x:Name="ToursList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Width="250" Margin="15" UniformCornerRadius="10" materialDesign:ElevationAssist.Elevation="Dp2">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="140"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Border Background="{DynamicResource PrimaryHueLightBrush}" Grid.Row="0">
                                        <materialDesign:PackIcon Kind="ImageOutline" Width="50" Height="50" 
                                                                 HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                                 Opacity="0.3"/>
                                    </Border>

                                    <StackPanel Grid.Row="1" Margin="15">
                                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding StartDate, StringFormat='{}Date: {0:dd.MM.yyyy}'}" Opacity="0.6" Margin="0,5"/>

                                        <Separator Margin="0,10"/>

                                        <DockPanel>
                                            <TextBlock Text="{Binding Price, StringFormat='{}{0}$'}" FontSize="20" 
                                                       FontWeight="Black" Foreground="{DynamicResource SecondaryHueMidBrush}" 
                                                       VerticalAlignment="Center"/>

                                            <Button Content="BOOK" 
                                                    Click="BtnBook_Click"
                                                    HorizontalAlignment="Right" 
                                                    Style="{StaticResource MaterialDesignRaisedSecondaryButton}" 
                                                    Height="32" 
                                                    MinWidth="80"
                                                    Tag="{Binding Id}"/>
                                        </DockPanel>
                                    </StackPanel>
                                </Grid>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </materialDesign:DialogHost>
</Page>